<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Calorie Counter</title>

  <!-- Tailwind (dev only) -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Firebase -->
  <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
  <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-auth.js"></script>
  <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-firestore.js"></script>

  <script src="firebase-config.js"></script>
</head>

<body class="bg-gray-100">

  <!-- DATE PICKER -->
  <div class="p-4 bg-white shadow">
    <label class="text-sm font-medium">Date</label>
    <input id="dateInput" type="date" class="border p-2 rounded w-full mt-1" />
  </div>

  <!-- DAILY TOTAL -->
  <div class="p-4 bg-white shadow mt-2 flex justify-between">
    <div class="text-sm font-medium">Daily Total</div>
    <div id="daily-total" class="font-bold">0</div>
  </div>

  <!-- MEAL CONTAINER (REQUIRED) -->
  <div id="meal-container" class="mt-3"></div>

  <!-- ADD FOOD BUTTON -->
  <div class="fixed bottom-4 right-4">
    <button onclick="showAddFood()"
      class="bg-purple-600 text-white px-4 py-2 rounded shadow">
      Add Food
    </button>
  </div>

  <!-- ADD FOOD MODAL -->
  <div id="addFoodModal" class="hidden fixed inset-0 bg-white z-40 flex flex-col">

    <div class="bg-purple-600 text-white p-3 flex items-center gap-2">
      <button onclick="closeAddFood()" class="text-lg p-0.5">✕</button>
      <h2 class="font-bold text-sm">Add Food</h2>
    </div>

    <div class="p-4 space-y-4 overflow-y-auto">

      <!-- FOOD SEARCH -->
      <div>
        <label class="text-sm font-medium">Search Foods</label>
        <div class="flex gap-2 mt-1">
          <input id="foodSearch" type="text" class="border p-2 rounded w-full" />
          <button onclick="voiceInput('foodSearch',false,voiceFilterFoods)"
            class="px-3 py-2 bg-gray-200 rounded">🎤</button>
        </div>
        <div id="foodSearchResults" class="border rounded mt-1"></div>
      </div>

      <!-- FOOD NAME -->
      <div>
        <label class="text-sm font-medium">Food</label>
        <input id="addFoodFood" type="text" class="border p-2 rounded w-full" />
      </div>

      <!-- COMMENT -->
      <div>
        <label class="text-sm font-medium">Comment</label>
        <input id="addFoodComment" type="text" class="border p-2 rounded w-full" />
      </div>

      <!-- UNIT -->
      <div>
        <label class="text-sm font-medium">Unit</label>
        <input id="addFoodUnit" type="text" class="border p-2 rounded w-full" />
      </div>

      <!-- CALORIES PER SERVING (READ ONLY) -->
      <div>
        <label class="text-sm font-medium">Calories Per Serving</label>
        <input id="addFoodCaloriesPerServing" type="number"
          class="border p-2 rounded w-full bg-gray-100" readonly />
      </div>

      <!-- SERVING SIZE -->
      <div>
        <label class="text-sm font-medium">Serving Size</label>
        <input id="addFoodServingSize" type="number" class="border p-2 rounded w-full" />
      </div>

      <!-- CALORIES FOR SERVING (READ ONLY) -->
      <div>
        <label class="text-sm font-medium">Calories For This Serving</label>
        <input id="addFoodCaloriesForServing" type="number"
          class="border p-2 rounded w-full bg-gray-100" readonly />
      </div>

      <!-- SAVE / CLEAR -->
      <div class="flex gap-3 pt-4">
        <button id="addFoodSave" class="flex-1 bg-purple-600 text-white py-2 rounded">
          Save
        </button>
        <button id="addFoodClear" class="flex-1 bg-gray-300 py-2 rounded">
          Clear
        </button>
      </div>

    </div>
  </div>

  <!-- APP JS -->
  <script src="app.js"></script>

</body>
</html>
